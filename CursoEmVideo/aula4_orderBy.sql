use cadastro;

# ordenar padrão (asc)
select * from cursos
order by nome;

# ordernar crescente
select * from cursos
order by nome asc;

# ordernar decrescente
select * from cursos
order by nome desc;